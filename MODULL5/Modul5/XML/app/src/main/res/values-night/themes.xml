<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MovieList" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.MovieList" parent="Base.Theme.MovieList" />

    <!-- TEMA BARU UNTUK NO ACTION BAR -->
    <style name="Theme.MovieList.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tidak ada ActionBar -->
    </style>
</resources>